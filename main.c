#include "monty.c"

/**
 * main - main function
 * @argc: argument count
 * @argv: vector count
 * Return: success or failure
 */
int main(int argc, char *argv[])

{
if (argc != 2 )
fprintf(stderr, USUSAGE: monty fileAGE: monty file)
return (EXIT_FAILURE)

}
